{
  // Editor convenience only. Builds/typecheck run per-package via turbo;
  // each package owns its own tsconfig extending @lazuli/tsconfig.
  "extends": "./tooling/tsconfig/base.json",
  "include": [],
  "exclude": ["node_modules"]
}
